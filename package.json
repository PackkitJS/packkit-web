{
  "name": "packkit-web",
  "version": "0.1.0",
  "private": true,
  "description": "Packkit web configurator — pick a language, configure a project, download it as a zip. Runs in the browser.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --serve",
    "check": "node scripts/build.mjs && node scripts/check.mjs"
  },
  "author": "DanMat <dannymatthew@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@packkit/core": "^0.7.0",
    "create-packkit": "^4.5.0",
    "create-packkit-go": "^0.5.0",
    "create-packkit-py": "^2.4.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
